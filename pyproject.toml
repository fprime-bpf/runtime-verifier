[project]
name = "bpf-runtime-verifier"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["z3-solver", "pyyaml>=6.0"]

[tool.pyright]
pythonVersion = "3.13"
venvPath = "."
venv = ".venv"
